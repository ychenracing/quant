# quant current recovery entry

Continue `ychenracing/quant` PR #1 under alpha-first authority. Maximize return
capability while preserving original acceptance, frozen identities, execution
boundaries and all failures. Do not return to risk patching, score/funding
micro-tuning, extra gates, neighboring searches, per-pool splicing or private
reference implementation use.

## Live identities

- `main@966f7428658431c9da5a6fcaf7d9b9d5c446f2cd`;
- PR #1 OPEN/Draft/unmerged at
  `research/independent-tech@efffb9a428258e921feaff125011cba46223bb18`;
- latest research result before status save:
  `research/expectation-learning@39695299d8c7cdab27c1818f861ee6111715415e`.

Read `research/ACTIVE_WORK.md` first. Writer is released after the status save;
no accepted production tree or pending economic run exists.

## Latest closure

`explosive_campaign_accelerator` is permanently rejected. It changed wealth by
-67.33%/-25.08%/+5.93% and worsened MDD by 6.30/8.42/6.88pp on the three core
scopes. It fired 304/117/271 days, proving that a generic top-decile breakout
and sector-confirmation proxy does not recover sparse reference launch meaning.
Do not tune its decile, windows, volume, peer or capacity rule and do not splice
the removal row. Source/result commits are `0cc83f09` / `39695299`.

Reference lifecycle attribution also closes importing its exits: passive NAV
usually rose after `dd_half` and `dd_clear`. Exact current-parent replay shows
the strongest descriptive entries were usually already owned or recognized,
or were blocked by actual protection/capacity. Broad admission was redundant;
protection cannot be bypassed and capacity/handover wrappers already failed.

Detailed evidence is
`renewed-continuation/alpha-gap/3969529-lifecycle-and-accelerator-rejection.md`.

## Direct continuation

Do not add another trend-book wrapper, gate, slot, reentry permission or broad
ownership transition. If independent evidence supports further work, it must be
a standalone campaign-epoch decision path whose sparse launches and persistent
actual-unit lifecycle explain alpha across union, common-five and removal before
implementation. Otherwise reject the direction without a replay. Preserve all
closed families listed in ACTIVE_WORK, including campaign_payoff, joint_funding,
confirmed_shock, inventory corrections, profit_trend_shield, leader slots,
handover, persistent/early ownership, breadth, sector ownership and accelerator.

Economic acceptance remains `NOT_MET`. Keep PR #1 Draft/unmerged; merge only a
single identical candidate that passes every economic, engineering and
repository-protection requirement.
