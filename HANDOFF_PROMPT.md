# HANDOFF_PROMPT — quant 独立科技股策略继续优化与验收

请接管 GitHub 仓库 `ychenracing/quant`，继续原任务。这不是重新立项，不是只做分析，也不是让用户再次确认授权。先最小核验实时现场和证据身份，恢复已保存成果，然后继续实施、归因、验证、保全。允许推翻当前独立策略重写；不要把计划、局部测试、诊断报告、提交或发布 main 当成经济目标完成。

本次交接核验日期：2026-09-14，时区 Asia/Tokyo。下列 SHA 是核验快照；新会话必须检查有无后续提交。用户最后要求“把工作交接出来，prompt给我”，旧会话交付后不再推进策略或新增调度；新会话按原授权继续。

## 用户目标、授权和不可破坏边界

目标是在 `trades` 中 `chatgpt`、`trae`、`dumate`、`workbuddy` 四套策略的只读研究基础上，独立开发更强的新策略，最终交付 quant 的 main。不得以四套中的任何一套为源码底座，不复制其函数、继承其引擎或把其代码改名包装。已经独立编写的 `src/techquant` 可以继续改进或重写；四套原代码仅用于理解机制、比较和隔离原生回放，不是生产依赖。

只研究 2023 年 1 月起的 A 股科技股，不扩展消费、医药或 2023 年之前的数据与预热。现金多头、日频、盘后信号、下一交易日执行、人工核对；不加杠杆、不做空、不接券商自动下单。移除某股票时不得继续让该股通过模型训练、宽度、排名或其他隐性输入影响剩余池。

经济目标保持不变：收益至少持平四套中最优者并争取超过；牛市尽量充分持有，2026 年 6 月底至 8 月下跌尽量少损失；风险识别和实际规避优于四套；移除关键盈利标的后仍有强竞争力；支持四套涉及的各原池及任意非空组合、不同数量组合，同时控制交易次数。少数标的贡献主要收益可以接受，但不得极端依赖它们。不能拿单池高收益掩盖其他池退化。

用户已授权直接实施、必要时重写、推送代码与研究中间结果，并在真实验收后完成 main 交付，不再询问是否执行或是否继续。局部失败后继续定位原因和尝试有依据的修复，不以“未达标”作为一轮实施的结束理由。但授权不允许伪造证据、隐瞒失败、回填未来数据、无限调参后把已知历史称作样本外，或保证未经证明的全面最优。

不引入产品版本号、阶段号、v2/v3、phase/stage 命名、临时兼容分支或多代生产入口。保持一个清楚的生产实现及一致文档。Git SHA、数据哈希、实验身份和失败归档仍必须保留，它们不是要删除的“过渡期产物”。不要为了命名整洁删除溯源记录。

需要充分的回测、中文架构/策略/参数/操作文档和解释设计意图的必要注释；优先简洁可维护，不靠过多参数、测试或 CI 门禁制造工程复杂度。当前普通 CI 为一个离线正确性作业、4 分钟超时；完整研究独立运行，现有显式工作流上限 9 分钟，不扩成常规经济矩阵。不等待长流程来替代推进可独立完成的工作。

不能改变旧实验冻结的候选、窗口、统计、费用、seed 或验收来通过测试。重写已被授权，新的合理假设可另行预登记并记录全部试验，不必重新找用户审批；不得反向修改旧合同。不得套用 uquant/trade 等其他任务的 15 倍、40 单、10% 浮动或其他豁免。本任务尚未查到统一绝对成交数和绝对回撤硬阈值，不能把别处数字冒充本任务约定。

## 已核验的远端现场

仓库为公开仓库，默认分支 main。GitHub 读取成功。分支快照如下：

| 分支 | SHA | 用途或状态 |
|---|---|---|
| `main` | `bbc002449093e458868a8748687df023958ba283` | 已发布独立代码；经济目标未通过 |
| `delivery/verified-independent-tech` | `bbc002449093e458868a8748687df023958ba283` | 同一交付源码 |
| `research/expectation-learning` | `726c2538ea53f9c522132eed5f4e529a7841041f` | 最新查到的自适应预测及可恢复研究实现，优先核验的续作入口 |
| `research/causal-protection` | `fe1fb2da3f0e6f4d9244118788efbd64cf02292e` | 因果风险、趋势持有和多组失败研究 |
| `research/trend-participation` | `d42c09ec3041a2e7c90b9a9629761d751b1c7056` | 高参与度失败候选及检查点 |
| `research/independent-tech` | `4fa306fdcde2f9a77d941200ef7ff2f988c045e5` | 原 PR #1，另有独立 ownership 等研究 |
| `research/retention-diagnostic` | `a183953ad4281f76b48ec04e4e457e4e8814e209` | 较早持有诊断，不当成最新生产底座 |
| `research/technology-strategy` | `3f89e5c357da3126fedc20f17ee97cfa8d9cdb15` | 早期设计与公开数据采集 |
| `research/evidence` | `b0ab27acfdd36408e4b0fa804986e60c09b0b9c2` | 写入本交接文件前的耐久证据分支快照 |

main 的完整 Git 树为 `41393b859363f73216149efc673584cc2424907c`，生产包 SHA256 为 `bdff426581e521713f28939e79f5aaa967273d9521bb881f0d5b64a2e121ca9f`。

原 PR #1：`Independent technology-equity strategy and reproducible research`，仍 open / Draft，head 为 `research/independent-tech`。不要无故另建替代 PR或覆盖其他研究分支。PR 返回的 base SHA 仍为旧 `234cc5729455ce5440b0a5307aadbcb9c1c0bbc4`，与实时 main 不同；恢复以分支 ref 为准，不能依据旧 PR base 回退 main。

`expectation-learning` 相对 main ahead 32、behind 0；其 HEAD 提交时间为 2026-09-14 03:28:44 UTC，提交主题是增加可恢复固定网格选择和匹配回顾性评价。原 PR 分支相对 main 已分叉（ahead 9、behind 3），不是可以直接无脑覆盖 main 的副本。正常整合已有改动，禁止强推覆盖并行成果。读取实际存在的 AGENTS.md；已读源码快照及该研究现场记录未发现根目录 AGENTS.md，不能编造其内容，现有 `docs/RESEARCH_CONTRACT.md` 和项目约束仍适用。

## 直接续接入口：已有代码，不要从报告重新造一遍

优先读取 `research/expectation-learning` 的：

- `research/EXPECTATION_WORK.md`、`research/adaptive_expectation_contract.json`。
- `research/expectation.py`、`research/expectation_study.py`。
- `src/techquant/policy.py`、`src/techquant/engine.py`。
- `tests/test_policy.py`、`tests/test_expectation.py`。

该分支已经增加显式盘后决策接口，输入真实模拟成交后的持仓/现金/日序号，避免用模块全局函数替换注入研究逻辑。已经写入因果特征、成熟标签、按历史逐月滚动的加权 ridge 预测、实际持仓单位保留、保护性卖出锁存/重试，以及可恢复选优 runner。代码落地不等于这些行为或经济改善已验证。

已冻结候选为 `horizon=[10,20] × shrinkage=[0.1,1.0,10.0] × positions=[2,4]`，共 12 个；选优仅截至 2025-12-31，在联合池、共同五股、联合移除三龙头三个范围筛选。候选排序先看每池财富、回撤及成交笔数相对 incumbent 的最大退化，再按已声明目标打破平局；只是必要筛选，不能替代四套对照和泛化总验收。

标签只可使用在当时决策日已完全成熟的数据：`t + horizon <= signal_day`；标准化和拟合不能越界。每 20 个交易日拟合，最多 504 个历史交易日，日期权重半衰期 120 日；样本不足使用明确的因果先验。详情及退出、仓位、暂停等常量以原冻结 JSON 为准，不能在失败后悄悄改值。

交接时未找到该 HEAD 的 GitHub check-runs（返回 0）；也未核验到其候选选择、完整回顾性矩阵或正式经济通过证据。因此不得声称最新研究已通过测试、已改善经济表现或已合入 main。先寻找与本 SHA/runner 等价的可恢复输出；没有时从必要正确性及冻结选择继续，而不是重跑所有旧研究。

以下命令来自已读 runner，工作目录必须是真实、身份一致的源码 checkout，输入路径自行从耐久证据恢复，不依赖旧会话路径：

```bash
python -m pip install -e .
export PYTHONPATH="$PWD/src:$PWD"
export OPENBLAS_NUM_THREADS=1
export OMP_NUM_THREADS=1
export QUANT_SOURCE_COMMIT="$(git rev-parse HEAD)"
python -m unittest discover -s tests -v

# 先核验下列输入路径对应的冻结数据；已有匹配结果时让 runner 校验后恢复。
python -m research.expectation_study \
  --data evidence/inputs/market --supplement evidence/inputs/supplement \
  --output runs/expectation-selection
python -m research.expectation_study \
  --data evidence/inputs/market --supplement evidence/inputs/supplement \
  --selection runs/expectation-selection/selection.json \
  --output runs/expectation-evaluation
```

runner 保存 `plan.json`、`baselines.json`、`trials.csv`、`objectives.json`、`selection.json`、逐笔/逐日结果和预测拟合信息；评价产生 `matrix.csv`、`status.json`。支持已有目录内身份完全一致的结果复用，不是忽略身份的强制续跑。源码、配置、runtime、data、runner 或输出 plan 不符时保留旧结果，不能改 identity 伪装匹配。`CORE_NONREGRESSION_ONLY` 和 `MEASURED_NOT_UNIVERSAL_ACCEPTANCE` 都不是总目标 PASS。

## main 已有经济结果：保留作为基线，不重复全量重跑

冻结数据：2023-01-03 至 2026-09-11，34 只科技股、896 个交易日，初始资金 200 万元。数据 SHA256：`d9ded1d933f292933aa09145bc36a5696421c43dbf266fce018a34ef0b9f3f9b`。财富含本金，回撤以下以正数损失幅度表示。

| 股票池 | main 财富 | 同引擎买入持有财富 | main 最大回撤 | 买入持有最大回撤 | main 成交笔数 |
|---|---:|---:|---:|---:|---:|
| 共同五股 | 10.824786 | 29.062957 | 33.07% | 52.39% | 139 |
| 34 股联合池 | 5.624793 | 9.102309 | 32.07% | 39.88% | 202 |
| 联合移除三只光通信龙头 | 2.246934 | 5.716080 | 45.87% | 45.69% | 209 |

共同五股为 `sz300308,sz300502,sz300394,sh688008,sh603986`；三龙头移除为同时移除 `sz300308,sz300502,sz300394`。

| 股票池 | 截至 2026-06-30 财富 | 2026-06-22 至 08-31 收益 | 2026-07-01 至 08-31 收益 |
|---|---:|---:|---:|
| 共同五股 | 12.674859 | -4.56% | -14.60% |
| 联合池 | 7.166319 | -17.04% | -21.51% |
| 联合移除三龙头 | 2.887965 | -14.10% | -22.20% |

三个池压力段首次预算下调是 2026-07-02 收盘，关联卖出为 07-03。不是六月底提前躲避风险。两种下跌起点都要报告；子区间收益包含首日相对前一收盘的变化，不准用换窗口隐藏损失。

当前源码完整有限研究为 210 场景、437 次回放；197 个同执行买入持有比较仅 3 个同时财富不低、回撤不高，财富保留比例中位数 43.05%、最低 4.62%。覆盖原池、单股、逐一/联合/行业移除、共同五股全部 31 个非空子集、确定性随机子集、数量 1—34、成本/延迟及参数邻域。不能把这些称为全部 34 股共 17,179,869,183 个非空组合已穷举或未来保证。

四套原生共同五股全期财富/最大回撤：chatgpt `1.778233 / 29.81%`；trae `10.674862 / 33.11%`；dumate `1.465010 / 20.69%`；workbuddy `20.221073 / 25.33%`。27 个本地原生场景中 18 个覆盖完整、4 个超时、5 个覆盖失效。它们保留各自原生成交和费用，不是严格统一执行排名；不能算无效或超时为新策略获胜。workbuddy 的 20.221073 倍不是所有池共同适用的硬阈值，应逐池恢复其适用对照。

## 后续已做过的研究和新发现：必须复用失败记录

原 PR 分支 `evidence/ownership_checkpoint.json`：54 候选、165 次训练回放，状态 `REJECTED_AS_UNIVERSAL_REPLACEMENT`、canonical=false。选中候选的联合池财富/回撤为 `14.224479 / 49.09%`，共同五股 `8.415690 / 34.12%`，联合移除三龙头 `4.748349 / 51.93%`；收益与风险没有共同改善。记录中的归因对照仅是诊断，不证明当前 HEAD。

趋势参与分支 `research/participation_checkpoint.json`：24 候选、75 次训练回放，`REJECTED_NOT_PROMOTED`。联合池约 `11.113211 / 70.03%`，共同五股 `10.868664 / 51.19%`，移除三龙头 `7.819671 / 68.25%`；联合池七八月约 -41.15%。不能再次仅用增加仓位来弥补收益。该文件记载 45 项离线测试通过，是该实验记录，不是最新预测分支的验证。

因果风控分支 `research/CAUSAL_RESEARCH_STATUS.md` 已保存以下研究摘要；全部是绑定各实际适配器哈希的诊断，不是该分支 HEAD 的正式总验收：

| 机制 | 联合池财富/回撤 | 共同五股财富/回撤 | 移除三龙头财富/回撤 |
|---|---|---|---|
| 高低水位反转 | 13.058341 / 59.26% | 9.134236 / 49.65% | 4.337454 / 75.77% |
| 现金保留突破 | 8.340927 / 46.72% | 21.993765 / 46.45% | 1.136753 / 68.92% |
| 持续参考组合保护 | 4.470905 / 42.35% | 8.454547 / 31.94% | 0.424281 / 65.57% |
| 历史新高入场 | 2.402835 / 42.50% | 7.989597 / 38.91% | 1.575839 / 48.69% |
| 趋势轮换 | 5.566429 / 50.75% | 19.707915 / 37.10% | 2.505302 / 68.35% |
| 被动参考保护并保留实际单位 | 6.376130 / 35.07% | 19.838468 / 48.53% | 4.178455 / 37.04% |
| 下行波动保护 | 3.572700 / 43.29% | 3.591381 / 44.14% | 2.280040 / 40.24% |
| 独立资金分配，仅诊断 | 3.963245 / 26.06% | 12.354969 / 28.95% | 2.555656 / 36.30% |

独立资金分配错误地按子账户而非整账户计算 1% 最小成交金额，且成交数为 606、100、544，不能当作公平低回撤优胜。被动参考适配器曾把剩余初始预算误作每日新动作造成再平衡；修正后共同五股成交由 337 降至 90，但经济仍未联合达标，旧错误结果须保留并标注。

同一研究状态还记录一个原生 workbuddy 库存异常：2025-04-28 共同五股中新易盛期初可卖库存为零，当天买入后又卖出 52,100 个模拟股份单位，价格 45.546408、金额约 2,372,967.8568 元。原池重复记录不是第二个独立事件。原 `trades.csv` SHA256 为 `a9620cef60d7f18623af3d676ac7aaaa29d7de9613712812764998eecb406adb`。这是远端审计记录，本次交接未重新逐笔验算；续作需复核可卖库存、适用 T+1 与财富影响。不得照搬违规模拟，也不得借此删掉原报 20.221073 倍或下调用户收益目标。原生原报、执行有效性、统一执行对照分别呈现。

上述摘要和部分源码已经远端保存，但完整原始试验账本/曲线是否全部远端保存未核验。有些 checkpoint 只写“locally retained”，不能当成已完整上传。先查原分支、PR 评论、Actions 产物及耐久证据；找不到的明确列作缺口，不伪造，不无差别重跑所有失败族。

## 耐久数据、源码及证据恢复

优先从 GitHub 恢复，不要求用户重新上传附件。`research/evidence` 的 `LATEST.json` 已实读：指向 main 的 `bbc002449093e458868a8748687df023958ba283`，run `34794779702`，economic_acceptance=`NOT_PASSED`。不是最新预测研究的结果。

正式已发布研究路径：

```text
published/bbc002449093e458868a8748687df023958ba283/34794779702/
published/bbc002449093e458868a8748687df023958ba283/local-native/verification.json
```

按 `docs/OPERATIONS.md` 从第一处恢复分卷、`SHA256SUMS`、`receipt.json`、`SOURCE_IDENTITY.json` 和完整清单，核对后拼接解压。包内 `evidence/inputs/market`、`evidence/inputs/supplement`、`evidence/inputs/indices` 是冻结输入；不得重下载变化后的行情冒充它们。历史交付报告记载 6,048 个清单文件核验，本次只重新检查了附件整包 SHA256，没有重新回放全矩阵。

本会话附件已核实存在，以下仅为可用备援，不作为新会话必需上传物：

- `quant-main-bbc0024-evidence.tar.gz`：SHA256 `6f7864e8f60001790806dcedb2fb7abc5960fb992c936ee9ea2be02069f3f353`，与耐久主证据相应的整包身份。
- `quant-main-bbc0024-native-evidence.zip`：SHA256 `0a03c52f24dd08c2488b897b6e0a3f3d7211c467c49300645e1d722894ee620f`。267 个原生产物的完整伴随包；此前只确认远端有摘要和校验记录，不能假称完整 ZIP 已推送。
- `quant-historical-evidence-567a2b6.zip`：SHA256 `ca5c9ca2b38fbea3b54af4d93cc089c05a3091c046a42221d7a92d92fa4f0a5d`，独立历史失败证据。

旧 `d14ef93...` 本地交付、`567a2b6...` 历史源码和新 main/研究分支是不同身份。不能因为数字相似便改标签复用。`research/evidence/independent-optimization/` 本次只查到一个 `source-00.b64`，未核验完整包，不得假设所有后续实验都在其中。

四个只读参考固定到 `ychenracing/trades@5575d1b1b79fab405b92cfb7d164c7054a8bd826`；较早下载档案为 `c3d838ce6349f8bdf0ec2988836212563aaa10bc`，此前已对照四个目标目录无改动，续作需维护这一等价证据。目录和池定义从 `research/catalog.json`、`research/native.py` 读取，不猜路径。`trae_24` 是原名称但实际包含 26 个证券，不按名字截成 24 个。

quant 公开 Actions 的既有流程不能读取私有 trades，已用 `--without-native` 明确记录 27 项 `REFERENCE_CHECKOUT_UNAVAILABLE`，不算原生回放成功。授权本地/插件取参考源码后可跑隔离对照，不能把私有四套源码公开复制到 quant，也不能扩大凭据权限绕过安全边界。缺少原生完整包时先恢复远端摘要和已授权参考；只重做无法恢复而对当前判断必要的部分。

## 工程、任务和本地状态

main 的 39 项必要离线测试通过属于既有交付记录；本次已核验附带测试日志和远端对应检查。GitHub `correctness` run `34794779726` 和 `reproduce` run `34794779702` 对 main SHA 均 completed/success。研究执行成功不代表经济通过。旧 main 构建、CLI、编译和数据检查成功不能代替新预测分支测试。

交接核验时 quant Actions 的 in_progress=0、queued=0。本次当前容器未找到项目 Git checkout，也未找到 quant/pytest/研究执行进程；只有用户附件及本次交接文件。没有可确认的新未提交或未推送工作树改动，不等于证明旧 Work/Codex 容器没有残留成果。旧容器的工作区路径、进程、未推送补丁与未上传原始回放均待核验，不得写成“全部已远端保存”。没有检查或修改账户定时任务，也不能声称其他会话任务已全部取消。

本次直接网络读取 GitHub 因 DNS `Temporary failure in name resolution` 失败，但 GitHub 插件读取正常，且当前已发现 `create_file`、`update_file` 等写入能力。不要沿用旧回答“插件没有写入操作”作为结论。实际写权限以调用和回读为准；有合法 Git/已授权插件/API 替代路径就继续，不能凭工具清单说已经推送。写文件先读当前 blob，分支推进后核验 SHA；不得覆盖并行写入或绕过实际仓库保护。

## 新会话接手后直接执行

先读取实时 refs、原 PR #1 和上述恢复文件，保存并核验任何可见的未推送成果。优先在已有 `research/expectation-learning` 继续验证它已经落地的实现；这只是基于现存代码减少重复工作，不是认定该模型已最优。确认另一分支已有等价或更新成果时先整合，避免新开重复研究线。

最小正确性重点：默认路径等价；资金/权重/可卖库存与 T+1；真实成交后的风险状态；受阻卖出跨日重试；预测、标准化、成熟标签和拟合日期的前缀一致；移除标的后模型输入真正隔离；原有费用、流动性、涨跌停、最小成交金额保持不变。只补风险相关缺失测试，不制造大 CI 矩阵。

复用已冻结的预测研究协议和可恢复 runner。先在三个核心池定位选择/持有/止损/暂停/再入场分别贡献多少损失，比较财富、回撤、成交数、平均实际仓位与回撤期净值，不再盲目提高仓位或叠加止损。平均敞口约 42%—45%只是旧策略观察，不能未经归因就当作全部损失根因。

有支持的机制继续改进，失败则保留完整证据并修复被定位的因果问题；需要新结构时，先声明假设及有限候选再跑，不能偷偷扩大旧网格或反向利用已知 2026 年路径。2026 年已经被反复观察，只能称回顾性检验。

候选在必要核心筛选有实证改善后，补齐同源码、同配置、同数据、同宇宙、同成本/执行/seed/runtime 的完整必要评估：原四策略各池、关键及实际盈利者移除、行业移除、不同数量和固定随机子集、牛市/两个下跌窗、成本和延迟压力、参数邻域。风险识别比较必须同时披露事件定义、信号日、首次可执行日、实际卖出和敞口、误报、误卖成本、恢复时间；不能凭“第一次降预算更早”认定全面优胜。

任意非空池可运行与任意池经济最优必须分开验证；有限场景不能宣传为全组合证明，也不能据此自行把原目标改成“平均表现好即可”。文档明确覆盖边界和尚未证明的结论，同时继续推进能实际验证的改进。收益至少最优、风险/泛化全面超越仍是原目标，不降格成可运行交付。

每个有意义的代码或结果增量及时提交到既有研究分支，原始证据按源码/数据/runner 身份保存到耐久证据分支，回读核验。不要等到长任务结束才保存；也不要把每个 checkpoint 当成交接或完成。避免公开私有参考源码或凭据。满足真实经济验收、必要正确性和实际保护后，正常整合至 main 并核验源码与结果身份；不要再以“已发布 main，但经济没达到”作为完成答案。

最终文档保持一个当前架构和参数入口，写清策略优势的证据、局限、实际交易约束和可改进处。工程通过、回测通过和未来收益保证是三件不同的事；“业界顶尖”没有被独立验证，不能作为已认证结论。若会话技术容量将耗尽，按用户既有要求先远端保全再精简交接，不能让已完成工作再次只留在临时容器。
