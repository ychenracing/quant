# quant current recovery entry

Updated 2026-09-15 UTC. Continue `ychenracing/quant` original PR #1 under the
latest alpha-first authority. Maximize return capability while preserving the
original acceptance, frozen identities and execution boundaries. Do not return
to defensive patching, score/funding micro-tuning, extra gates, neighboring
parameter searches, per-pool splicing or private-reference implementation use.

## Live identities

- `main@966f7428658431c9da5a6fcaf7d9b9d5c446f2cd`.
- PR #1 is OPEN, Draft and unmerged; implementation head
  `research/independent-tech@efffb9a428258e921feaff125011cba46223bb18`.
- Latest measured research source:
  `research/expectation-learning@2c53d24a0fbc1c44fcccf16ab2634434258b41ca`,
  tree `94ccd0a07f32783178ec4b860ef0e6cde9073a8e`.
- Hosted economic run:
  [35006251924](https://github.com/ychenracing/quant/actions/runs/35006251924);
  correctness run:
  [35006251956](https://github.com/ychenracing/quant/actions/runs/35006251956).

The source-branch `research/ACTIVE_WORK.md` must be reread for writer state.
There is no pending hosted run or unpublished source after the closure below.

## Latest closure: leader-anchor marginal slot

The fixed treatment always preserved the parent's strongest currently
admissible individual. Only the marginal second slot preferred its strongest
generic-sector peer, falling back to the unchanged parent second candidate.
There was no score/weight/threshold/risk/data/execution change.

Hosted fixed pair:

- union: `4.7441484288x -> 3.0374476183x` (-35.97%),
  MDD `30.5579% -> 33.8044%`;
- common-five: `7.9041830487x -> 8.6435865645x` (+9.35%),
  MDD `39.1356% -> 39.7191%`;
- joint leader-removal: `1.1546791489x -> 1.3319359407x` (+15.35%),
  MDD `35.3310% -> 31.0278%`.

Status is `REJECTED_PAIRED_SCREEN`; treatment 2026 runs = 0. Archive
SHA-256:
`b1d41a619a672a8d005231bd3f9e00ab8770abb768ed19cb9f751755f378f4c9`.
Selection SHA-256:
`15c4d7bc71b05fc293e5258b7b6ada60684cb19c4f7fcec652a4d34b82202ecd`.
Detailed evidence:
`renewed-continuation/alpha-gap/2c53d24-leader-anchor-slots-rejection.md`.

Do not tune companion rank, sector preference, fallback or nearby parameters,
and do not repackage this candidate.

## New alpha-gap finding

The marginal-slot mechanism successfully improved the two narrower pools, but
union's relative loss formed mainly in 2023 Q2. At 2023-05-18/19 its causal
state correctly identified `sz300308` and `sz300502` as the two-name
campaign. The control entered both from the 2023-05-19 signal and realized
about +1.058m/+0.272m. Treatment did not: earlier path choices left legacy
funded inventory/obligations in the book, delaying campaign ownership until
June. Its aggregate symbol campaign PnL was lower than control by about
1.705m on `sz300502` and 1.446m on `sz300308`.

Therefore the next bottleneck is not another admission score/filter. The
correct campaign can already be discovered; stale ownership blocks timely
handover.

## Direct continuation

A distinct next hypothesis is a causal campaign handover:

1. Keep the unchanged parent admission universe and ranking unrestricted.
2. Only when the parent's two highest currently admissible names already form
   one generic-sector cohort, release funded holdings outside that cohort so
   actual fills can free capacity for next-session ownership.
3. Do not bypass T+1, actual-share inventory, protective obligations, cash,
   costs, slippage, liquidity or risk. A requested release is not a completed
   fill.
4. Use no ticker/date/sector/pool branch, numerical threshold, persistence
   search or reference transaction at runtime.
5. Before implementation, quantify how often complete cohorts arrive while
   out-of-cohort inventory consumes slots, and preregister one control/treatment
   rule. Reject without neighbors if any core wealth declines, union fails to
   strictly improve, or no scope strictly improves.
6. Only a passing fixed pre-2026 pair may enter the original 2026/reference/
   pool/removal/cost/delay/stability/final acceptance matrix.

This is an exit/ownership state transition, not a new admission gate.

## Closed work: do not repeat

Preserve and do not reopen `campaign_payoff`, `joint_funding`,
`confirmed_shock`, inventory/T+1 corrections, native timing/fee probes,
`profit_trend_shield`, corrected `shock_reclaim`,
`causal_theme_campaign`, or `leader_anchor_slots`.

The binary theme campaign cut wealth 58.20%/32.01%/29.79% across the three
pools and is rejected. The leader-anchor result above is also rejected despite
two narrower-pool improvements. Existing coherent evidence reaches
`22.7716733036x` on common-five and `2.9915298718x` after joint leader
removal, but only `1.7481913470x` on union; never splice per-pool winners.
WorkBuddy versus quant attribution still shows the dominant gap is larger
profitable campaigns and longer holding, not defensive loss reduction.

## Immutable boundaries and completion

A-share AI/technology, daily cash long-only, after-close decision and
next-session execution, human verification, no broker auto-order, leverage or
shorting. Frozen data, benchmark, universe, seed, costs, liquidity, evidence
identity and metrics remain unchanged. Preserve every failure and never
combine best rows from different candidates.

Economic acceptance remains `NOT_MET`; no accepted production tree exists.
Keep PR #1 Draft/unmerged. Merge only when the same candidate satisfies all
effective economic, engineering and repository-protection requirements, then
verify the merge and main inclusion.
