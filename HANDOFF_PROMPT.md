# quant current recovery entry

Updated 2026-09-15 UTC. Continue `ychenracing/quant` original PR #1 under the alpha-first authority. Maximize return capability while preserving original acceptance, frozen identities and execution boundaries. Do not return to defensive patching, score/funding micro-tuning, extra gates, neighboring parameter searches, per-pool splicing or private-reference implementation use.

## Live identities

- `main@966f7428658431c9da5a6fcaf7d9b9d5c446f2cd`.
- PR #1 remains OPEN, Draft and unmerged; implementation head `research/independent-tech@efffb9a428258e921feaff125011cba46223bb18`.
- Latest research source/result record: `research/expectation-learning@ef5c6c78ce86d4ee38a1a1c49e004a445b4727cb`.
- Latest evidence note commit: `research/evidence@af915aad57ccbe72a6bf0de076ba545d684da497`.
- Frozen market SHA-256: `d9ded1d933f292933aa09145bc36a5696421c43dbf266fce018a34ef0b9f3f9b`.
- Pre-2026 selection SHA-256: `894230b20361fb826d58b27987e87146a090b9e894e7abf091121486f5293157`.

Reread source `research/ACTIVE_WORK.md` for writer state. No hosted run or unpublished source is pending after the closure below.

## Latest closure: persistent leader ownership

The fixed, preregistered `persistent_leader_ownership` candidate made the unchanged broad market state govern new units but made only unchanged security-specific brokenness authoritative for selling actual funded units. It was a standalone ownership state, not a shock-threshold neighbor or selective shield.

Pre-2026 fixed pair:

- union: `4.7441484288x -> 3.0566904348x` (-35.57%), MDD `30.56% -> 48.60%`;
- common-five: `7.9041830487x -> 10.9045257283x` (+37.96%), MDD `39.14% -> 44.60%`;
- joint leader-removal: `1.1546791489x -> 0.8432128697x` (-26.97%), MDD `35.33% -> 65.99%`.

Status is `REJECTED_PAIRED_SCREEN`; no 2026 or final matrix was run. Registration `fc329427c50c29e994f622330fbaa6d626f0d50b`; implementation `0970e07a6403c868e20deef02d85b6956d8f4906`; tests `7f9b09e9c08462b99d4043e16ed2cd00b1a1f1e6`; result record `ef5c6c78ce86d4ee38a1a1c49e004a445b4727cb`. Ordinary 87/87, focused 6/6, research 378 passed and 9 skipped. Maximum ledger residual `5.48e-9` CNY.

Detailed evidence: `renewed-continuation/alpha-gap/ef5c6c7-handover-exit-and-persistent-ownership.md`.

Do not search persistence windows, shock thresholds, partial-book variants or repackage this candidate. It proves ownership duration has material alpha leverage in common-five, but generic persistence also compounds broad-universe and removal distractors.

## Additional closed diagnostics

The proposed `campaign_handover` condition was reconstructed against actual fills before implementation. Actionable onset leader-minus-blocker mean 10-session spreads were only +0.45pp / -1.36pp / -0.31pp on union/common-five/removal, with near-random positive counts. It was rejected as diagnosis only and never became an economic candidate.

An exact first ordinary-exit audit found 10/6/9 newly latched security exits across the same scopes. The condition “currently profitable, same-sector peer currently parent-admissible, no account-risk cut” had zero activations in every scope. Attractive post-exit rallies therefore do not authorize that exit-delay rule.

Do not implement or tune either diagnostic.

## Direct continuation

The next high-information work is to attribute actual winning and losing episodes from the rejected persistent-ownership account at entry and over their first causally observed lifecycle. Compare contemporaneous price acceleration, sector participation and market state without using future outcomes at runtime. The required decision is whether a generic, prefix-causal campaign-quality state has enough cross-scope support to distinguish durable leaders from persistent distractors.

Only if that discriminant has adequate activation count and consistent economics should one new standalone entry-plus-lifecycle engine be preregistered. Otherwise stop adding wrappers to `trend_book` and redesign the alpha source itself. Any fixed candidate must still pass no-regression in all three core scopes with strict union improvement before 2026/final evaluation.

## Closed work: do not repeat

Preserve and do not reopen `campaign_payoff`, `joint_funding`, `confirmed_shock`, inventory/T+1 corrections, native timing/fee probes, `profit_trend_shield`, corrected `shock_reclaim`, `causal_theme_campaign`, `leader_anchor_slots`, `campaign_handover`, peer-confirmed exit delay or `persistent_leader_ownership`.

Existing coherent evidence reaches `22.7716733036x` on common-five and `2.9915298718x` after joint leader removal, but only `1.7481913470x` on union; never splice per-pool winners. WorkBuddy versus quant attribution still shows the dominant gap is larger profitable campaigns and longer holding, not defensive loss reduction.

## Immutable boundaries and completion

A-share AI/technology, daily cash long-only, after-close decision and next-session execution, human verification, no broker auto-order, leverage or shorting. Frozen data, benchmark, universe, seed, costs, liquidity, evidence identity and metrics remain unchanged. Preserve every failure and never combine best rows from different candidates.

Economic acceptance remains `NOT_MET`; no accepted production tree exists. Keep PR #1 Draft/unmerged. Merge only when the same candidate satisfies all effective economic, engineering and repository-protection requirements, then verify the merge and main inclusion.
