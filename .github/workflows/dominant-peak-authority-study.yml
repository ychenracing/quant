name: Dominant peak authority evidence
on:
  push:
    branches: [research/expectation-learning]
    paths:
      - .github/workflows/dominant-peak-authority-study.yml
      - research/offensive_dominant_peak_authority.py
      - research/test_offensive_dominant_peak_authority.py
      - research/offensive_dominant_peak_authority_contract.json
      - research/offensive_dominant_peak_authority_study.py
      - research/dominant-peak-authority-request.json
permissions:
  contents: write
concurrency:
  group: dominant-peak-authority-alpha
  cancel-in-progress: false
jobs:
  study:
    runs-on: ubuntu-latest
    timeout-minutes: 9
    env:
      PYTHONPATH: src:tests:.
      OPENBLAS_NUM_THREADS: '1'
      OMP_NUM_THREADS: '1'
      QUANT_SOURCE_COMMIT: ${{ github.sha }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13.5'
      - run: python -m pip install numpy==2.3.5 pandas==2.2.3 -r research/requirements.txt
      - name: Candidate TDD contract
        run: python -m unittest research.test_offensive_dominant_peak_authority -v
