# 自适应收益与下行风险研究现场

继续原授权的独立 A 股科技策略优化，沿用本分支已经冻结的 `research/adaptive_expectation_contract.json`。不复用四套参考项目的生产代码，不新增产品版本号或阶段编号，不改原始经济目标。

## 已核验

生产基底为 `bbc002449093e458868a8748687df023958ba283`，本地恢复的完整 Git 源码树为 `41393b859363f73216149efc673584cc2424907c`，与远端相同。研究分支接续点为 `313770bde09ab25b925b31dcd8d9935f1116222b`。所有旧研究、失败证据与原生参考结果保留，不覆盖其他研究分支。根目录 AGENTS.md 的实时读取返回不存在，使用仓库现有 RESEARCH_CONTRACT 和项目约束。

## 实施内容

- 为独立策略提供显式盘后决策接口，输入实际成交后的持仓、资金和当日序号；不再通过替换引擎模块全局函数注入研究逻辑。保持现有默认策略与成交、费用、T+1、流动性、涨跌停、最小金额规则不变。
- 独立实现已冻结的特征、成熟标签、逐月滚动加权 ridge 预测。拟合数据、标准化、标签截止点均不得越过当时决策日；移除股票时重新构建全部模型输入。
- 实现真实持仓单位保留、受阻保护性卖出锁存、退出后新资金暂停与有限成员轮换，避免把未成交目标当成实际持仓。
- 先验证默认路径等价、因果前缀、标签成熟边界、退出重试、现金与权重不变量；再执行冻结的十二个候选及三组选择范围，保留每个候选完整记录。
- 选择只看截至 2025-12-31 的记录；已被观察的 2026 年仍为回顾性检验。后续按原合同扩展组合、风险窗口、成本和执行延迟验证，不把局部筛选当成原目标达标。

## 保存与恢复

本文件是远端保存点，不是实施完成声明。后续实现与实测证据继续保存到当前研究分支，生产推广须与经济证据状态相符。恢复时先读取本文件、冻结合同及后续结果摘要，再核验源码、配置、数据与 runner 身份，禁止无差别重跑旧失败族。
