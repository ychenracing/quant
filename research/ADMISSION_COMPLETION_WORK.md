# 已选择新仓的预算补全研究

本研究继续原任务，不改变生产默认策略，不修改旧候选、经济验收或风险豁免。
预登记提交为 `0628f8db8e997dc0b332088bd0bad2b19cb9f779`，合同为
`admission_budget_completion_contract.json`，仅一个空参数候选。

## 恢复与依据

恢复并校验已完成的 observed_readiness（34839270406）、独立 readmission
（34838336120）和后来完成的 opportunity_reassignment（34861380791），没有重跑。
三个证据 ZIP、内部归档、清单、源码树、源模块依赖、选择及全部 54 个账户的
原始成交账本已经核验。最大逐日盈亏对账误差为 1.4901161193847656e-8 元。
三个选择最大退化分别为 0.06243165636706478、0.13532336185702162、
0.22389315267369225，均失败，不能作为总体验收通过。
三者共同五股全期财富分别为 9.402063343443142、9.344138547677543、
7.978817（最后一项展示值取六位小数；原始矩阵保留完整精度）。

observed_readiness 归档里的 quantity_gap 是原始修正父策略
`180640bff6d3a125cdf1b8928a6e0fc32a1d5f1a` 的既有诊断，不是新候选结果。
该父策略的既有真实持仓诊断是本次预登记依据。旧 funded 投影缺陷、
已完成的双交换设计失败、历史 leadership 传输校验失败均保留；本次没有
把这些内容标为 canonical，也没有恢复不可见的旧容器原件。

## 实现

先执行完全未修改的 quantity_obligation/support 父策略，再且仅对它已经选择的
新仓申请补全。先保留所有原始买单金额，再同时约束真实现金、总止损风险、
单股和行业上限；不使用预计卖出款，不增加标的或持仓数量，不扩大已持有股票，
不改变止损、保护性退出、原始经济减仓上限、恢复资格和真实净值风险历史。
每次补全保存实际持仓、原申请、补全申请及剩余资源；意图不是实际成交。

仅在研究 runner 里登记新族及完整依赖/意图证据；原有选择公式、窗口和旧族逻辑不变。
production、support_budget、quantity_obligation、funded_risk 及所有冻结合同字节不变。

## 已执行工程验证

十项新增测试先因缺失实现失败，然后全部通过。首次实现验证另发现一项
测试夹具把默认风险上限从 0 升到 0.4 错称下降；检查父 RiskState 后把夹具的
先前上限明确设为 1，再验证真实下降场景；没有修改策略来迎合错误夹具。
十项定向、134 项研究、77 项离线检查通过。涵盖资源预留、风险预算、行业/单股
限制、真实持仓/保护重试、父状态不变、因果前缀、延迟执行和缓存意图篡改拒绝。
对应原始日志和恢复审计单独保全，不能将这些测试当成经济通过。

## 当前执行入口

仅运行本次 singleton 的原定 pre-2026 三池选择及五窗口评价；不要因为旧
ACTIVE_WORK 或请求文件残留而重跑其他网格。本文件写入时新的经济结果尚未生成。
完成后以其准确源码 SHA 下的 selection、matrix、原始成交/意图及完整身份为准。
原 PR #1 保持 Draft；main 保持 bbc0024。只有同一生产候选满足原始联合经济目标
及仓库要求才可正常整合；不能因有限筛选通过就晋级。

2026 已反复观察，只能是回顾性检验。调整经济单位不是真实股份/税务账本；
原生私有参考的原件与有效覆盖仍有缺口，不能把 unavailable、无效或超时当作胜出。
