# 新开仓支撑结构检验

原任务续作，预登记提交为 `b796887bbc7a7e72251da0d726a3a3867b76420e`。
恢复入口为 `admission_structure_contract.json` 与 `nonlinear-request.json`。
仅比较原 observed_admission_completion 对照与新增支撑结构筛选，不改生产默认。

## 已恢复的结果

已校验最新联合分配源码 `4d0c9ec`、树 `6a3c8cd1db064f26310c5b4527913beea94016b0`
及已完成运行 `34910777906` 的 149 个清单成员与全部声明源码依赖。
选择器选回原对照：新联合分配使联合池训练期财富从 4.2311864966720991 倍降至
3.2298164383868051 倍，移除池回撤从 0.33029997565392888 升至 0.46927468198007016。
保留该失败，不将共同五股的局部改善拼接进其他池。请求的两个旧运行及中间四项
测量也已恢复，未重跑旧网格。四份 555 账户原审计的文件哈希与远端已保存记录一致，
无需再次宣称补传；它们不证明本候选。

## 假设与真实代价

对原对照实际成交持仓归因，无新组合回放。支撑低点下移的入场在三个池分别有
7、7、6 个持仓周期；已闭合周期按真实买入金额计算的平均收益均低于其余组。
但是联合池被筛选组仍贡献正利润，不能据此断言跳过它们就更赚钱。
原始逐日归因、所有持仓周期和统计均保留；这是小样本描述，不是因果效应或胜率保证。

只在原父策略允许增资的路径上，暂缓当前支撑低于一个 Config.fast 窗口以前支撑的
未持有股票。相等视为稳定；缺少早期估计时沿用父策略原冷启动条件，不引入更早数据。
不改变排名、已持仓加仓条件、非递减止损、实际风险历史、保护卖出及数量义务，
不从未成交卖单融资，不增加新的数值参数，也不组合已经失败的再入场和分配方案。
原始父模块、生产包和全部旧冻结合同字节保持不变。

## 工程验证与保存

新增 9 项定向测试先因缺失模块失败，再通过。首次执行还暴露两处测试夹具问题：
只修改收盘价导致非法 OHLC；CSV 缓存对全整数列恢复的 dtype 与内存不同。
修正的是合法扰动夹具和数值逐项精确比较，不是放宽策略条件。
87 项离线检查通过。研究检查在本地命令期限后只续跑未完成的 50 项，
154 加 50 共 204 项通过；中断日志保留，不伪装为一次完整成功运行。
新增诊断依赖登记后的定向检查及对应最终托管检查另以原始日志为准。

`records/admission_structure_verification.json`保存原始工程日志与恢复记录。
`admission_geometry.py`仅从固定原始成交账本还原本次观察，不运行新组合。
17 个派生文件的完整字节已与 `records/admission_structure_observations.json` 对齐。
托管测量会再次按这些不可变哈希恢复，并将实际 CSV 与核验记录一并持久化；
若字节不一致，须保留失败并明确非等价，不能改哈希伪装成功。

## 经济验收

仅执行新预登记的两候选、必要同源码对照与单一选中候选的五窗口评价。
这里写入时新经济结果尚未生成。以新源码 SHA 对应的原始 selection、matrix、
orders、intents、manifests 为准；选中原对照不算处理成功，核心筛选也不等于原始总目标。
原生参考、原池/移除/行业/固定子集/成本/延迟/邻域及真实风险规避要求不变。
2026 是回顾性历史，经济单位不是真实股份/公司行动/税务账本。原 PR #1 与 main
只在同一生产候选满足真实联合验收后正常整合，不修改目标或导入豁免。
