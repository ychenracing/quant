# Settled-campaign payoff alignment

Continue original quant PR1 under the user's latest instruction to keep implementing until the actual target is met. This is a distinct prospective hypothesis, not a retry of relative-leadership horizons or reopening an old budget.

Design: the exact unchanged same-scope control already needed for the pair supplies a shadow cash/inventory ledger. Extract only actually settled holding campaigns, using their entry-signal features and net cash payoff / actual buy notional. Learn only after final sale. Change capital priority, not protection, qualification, funding amounts or execution. Sparse selected samples and delayed large-win labels are known risks, not hidden assumptions.

Implementation plan:
1. Write and execute failing pure-numerical tests for settlement maturity, magnitude sensitivity, fallback, prefix causality, deterministic fitting and malformed samples. Implement research/campaign_payoff.py with one fixed ridge objective.
2. Add research/campaign_payoff_policy.py around the existing observed-admission owner. Validate exact reference identity; reuse its recorded control account instead of running an additional strategy. Preserve pending protection and original allocation hooks. Test exact disabled-control behavior, prefix/removal independence, cash, partial exits and delay/cost propagation.
3. Add research/campaign_payoff_study.py using existing ledger attribution, evidence serialization and paired_screen. Bind all transitive dependencies, full reference results, samples and fitted arrays. Reuse existing nonlinear workflow, with fail-closed routing to the new registered family.
4. Save implementation, tests and immutable registration before hosted measurement. Run affected tests then existing offline/research suite and six paired accounts. Advance only if the fixed screen passes; otherwise retain outputs and analyze actual closed/open campaign contributions without replaying a rejected treatment on 2026.
5. Save and read back source, raw outcomes and current continuation state; do not call one fixed experiment or a passing test suite project completion.

Old closed research records, main and original PR head remain untouched. Current writer: campaign-payoff continuation; no other active writer observed in live refs/comments/Actions at takeover. Recheck before every shared write.
