# 联合资金分配研究

继续原任务；预登记提交为 `2b62900b1e90436cf5d387d8eb1173f5c6a6f859`。
不重跑已完成的再入场、暂停持仓、原555账户矩阵和旧参数网格；不修改生产默认。
本次只比较完整 observed_admission_completion 对照与新的联合分配方法。

## 依据与边界

恢复最新924e0e3的精确源码和原始测量，全部568个恢复清单成员校验通过。
原始持仓归因不能支持全面放宽止损。以18901a4原解释器复原三个pre-2026实际
账户，两个额外排名诊断也没有跨池一致的均值改善：既不增加逆止损风险惩罚，
也不去掉原波动率惩罚。诊断只包含当时可知状态与随后完整成熟的20交易日
价格观察，不是反事实组合利润。两个诊断及其失败方向的原始CSV全部保留。

新的假设针对先排序、再按每新仓初始风险切片、最后补全的资金分配顺序。
入场、退出、评分、非递减止损、恢复、真实净值风险状态、实际库存和冻结费用
全部不变，只在原来可以新增资金的路径上，枚举不超过两个买入项的可行资金
分配。目标是现有评分的已分配资金加权和，不是预测收益保证。

现金、总止损风险、个股、行业、实际空余名额和原最低金额同时约束。
不卖出旧仓融资、不预测卖出款、不增加风险上限；保护减仓及未完成卖出优先。
原父模块和合同字节不变；内部分配器只在构造时建立，不替换运行中状态。
控制路径与原父账户的持仓目标、净值及逐笔订单精确一致。

## 验证与证据

9项定向测试、87项离线测试、195项研究测试以及编译通过。包含独立离散网格
下界、贪心反例、各资金约束、实际空余仓位、配置传入、时间前缀、移除标的、
完整父对照和意图缓存篡改拒绝。首次缺模块失败，以及恢复本地合同之前的
FileNotFoundError均保留；没有为修复恢复错误而修改合同。

`research/records/allocation_auction_verification.json`保存原始诊断脚本、CSV及
失败/成功工程日志，按文件格式说明base64解码、gzip解压后读取tar归档。
这仅是实施前后工程及历史观察，不是新经济通过证据。

此前尚未另存远端的四份555账户审计报告，以其不可变原始账户为压缩字典
保存在`research/records/fixed_account_audit_recovery.json`。运行
`python -m research.restore_recorded_audit --record <记录> --origin <原fixed-validation目录> --output <输出目录>`
可恢复原四文件；每个原文件的完整SHA256及逐字节等价已核验。恢复过程不是
重跑审计、不是补造原件，也不使e7341fc的历史报告变成当前候选证据。

## 当前测量

新请求仅运行预登记的两个候选、必要同源码对照和单一选中候选的原五窗口。
不按股票池拼接结果；选中原对照即表明新处理没有建立必要改进。
任一必要筛选通过都不能替代原生参考、原池/移除/行业/子集/成本/延迟/邻域
以及真实风险执行验收。2026仍是回顾性历史，经济单位仍不是实际股数和税务
账本。main与原PR#1不晋级，只有同一生产候选实际达标后才正常整合。
