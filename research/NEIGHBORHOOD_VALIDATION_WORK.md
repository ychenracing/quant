# 原始参数邻域的真实配置验证

本项续接原任务，不增加候选、不重新选优、不修改旧合同，不将参数邻域结果晋级默认值。
预登记为 `5ea64afe0e405d9d7cf0a651293ec44b7bcfd049`。

已恢复并逐项核验 e7341fc / 34876226498 的 3589 个证据清单成员；555 个账户覆盖
200 个场景，含别名的 85 个场景财富低于 incumbent。sample_8_0 全期财富
0.821487、最大回撤 68.5802%，其 2026 年前财富已为 0.423436，而 incumbent
为 0.770891。经济退化不是未观察 2026 或源代码上传问题；原联合目标仍未通过。

本次补齐旧验证明确缺失的十个原始参数邻域。沿 support、quantity obligation、
observed readiness、observed completion 的构造链显式传递不可变 Config，所有
特征、ATR、支持位与观察最小数在同一配置下建立，不在初始化后替换部分缓存。
不改变默认数值或任何 decide 逻辑；有限 runner 仅为该明确支持的族开放配置参数，
其他族拒绝未支持的配置覆盖，旧选择调用不传配置，仍采用原默认设置。

七项新增检查先失败再通过。对冻结三个核心池的实际库存逐日调用新 owner，
2688 次收盘目标与原记录误差全部为零，显式默认与省略默认的单位、原因、
风险额度及意图一致；这不是重新撮合旧研究，也不将旧 SHA 的经济结果改为新 SHA。
在保留并行 2ac1ecd 全部变更的集成树上，173 项研究、87 项离线检查通过。
编译和工作流内嵌 Python 语法检查通过。第一次全研究检查被工具调用超时中断，
保留其部分日志；后续完整检查通过，不将中断记作策略失败或完整通过。

只读取原 209 项计划中的十个 stability_not_reselection 配置，原样保留浮点值。
测量这十项及一个新源码默认对照，各自配同配置 incumbent；所有原时间窗、
费用、次日执行、实际持仓账本和保护义务不变。每个配置另作准确前缀验证，
同时比较净值、目标、实际成交和完整意图。设置未实际影响路径时明确报告其
在当前族中的惰性，不冒充有效调参旋钮。不开新网格，不按结果择优。

复用现有显式固定验证工作流，通过 operation 选择本项，禁止重跑已完成的
555 个账户。新源码、默认等价核验、完整原始账户、参数、失败和通过日志随
同一源身份保存，并追加到 evidence 分支后回读。此前本地重复固定 runner 已
被并行先完成的 e7341fc 替代，不发布为第二套生产或研究入口；其原文与日志
保留为未晋级历史。既有原生对照、泛化和真实经济缺口不因工程通过而豁免。

2026 是反复观察的回顾性数据；复权经济单位仍不是实际股份、公司行动和税务账本。
只有同一生产候选满足原始联合验收与仓库条件，才允许正常交付 main。
