# 固定候选的原始覆盖验证

本项继续原任务，只验证已选择的 `observed_admission_completion` 单例，不重新选优。
验证合同先独立保存于 `b4fc91c1ea3c98f65f3e7c4efdfd12f58f903749`。
并行的 funded_reentry 设计及其源文件、请求、旧合同保持不动。

## 已恢复的事实

快照校验补丁通过 GitHub 插件保存为 `655002c1e2fd40064bdc0e191bff913eda493901`；
其树 `6e1ceef360cdb22b98386825e6395345caed85fc` 与原本地 e8dddd7 完全一致，
保留 065f3d5 的执行金额下限修正及 18901a4 的策略实现。当前集成 87 项离线、
142 项原研究检查通过；这些不证明经济通过。

对已完成 run 34868483244 的全部 18 个账户逐笔重建开盘净值、现金、持仓及
保护性卖出身份，没有发现普通成交低于原有整账户 1% 开盘净值下限，逐日账本
对账误差为零。因此已知经济差距不是这条执行修正造成的，不能无条件重跑旧网格。
旧全期共同五股财富 10.598717085973565，低于 incumbent 10.824786016250098；
原生参考、完整覆盖及联合经济目标仍未通过。

## 验证方式

从已核验的原始证据包读取 209 项原始计划，不复制或改写场景生成规则。179 个
不同的非邻域账户上下文覆盖 199 项名义场景；保留别名场景，缓存相同真实账户。
原池还保留 equal-weight 对照。按固定规则补充移除本候选实际前三盈利贡献者；
这只是事后诊断，不影响选择。时间窗、手续费、滑点、执行延迟及数据均保留。

旧证据只在研究依赖、配置、数据、runtime 全部适用，生产改动限于已审阅的
两项正确性修正，而且逐笔确认新下限不触发时复用；保留原 SHA 和来源路径。
平台或其他身份不等价时生成新的源码绑定结果，不重标旧记录。没有测试到的
原生对照与 10 个参数邻域明确为 UNVERIFIED，不能将无效参数注入算作稳定性通过。
三池各三个固定日期另外核验实际净值、目标及成交的精确前缀一致。

本模块只登记一个独立、显式、9 分钟上限的覆盖验证工作流，不扩大普通 CI。
成功与失败均打包源码、参数、清单、执行日志、原始账本和意图，追加到证据分支
并回读校验。已完成的同身份账户从完整不可变缓存恢复；损坏缓存拒绝续写。

2026 是反复观察的回顾性历史；复权经济单位不是实际股份和公司行动税务账本。
有限测试不证明所有组合最优，工程或工作流成功不允许提前合并 main。
