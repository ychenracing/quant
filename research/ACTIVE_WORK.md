# Current quant continuation

Original task and PR #1 remain `NOT_COMPLETE / NOT_MET`. Continue alpha-first research while preserving original acceptance, frozen identities, execution constraints and every failed result. No defensive patching, ranking/funding micro-tuning, neighboring parameters, per-pool splicing or reference-source reuse.

## Current verified state

- main: `966f7428658431c9da5a6fcaf7d9b9d5c446f2cd`.
- PR #1: OPEN, Draft, unmerged; implementation head `efffb9a428258e921feaff125011cba46223bb18`.
- latest research result record: `ef5c6c78ce86d4ee38a1a1c49e004a445b4727cb`.
- authoritative recovery entry: latest `research/evidence:HANDOFF_PROMPT.md`.

## Latest closures

`persistent_leader_ownership` was preregistered at `fc329427c50c29e994f622330fbaa6d626f0d50b`, implemented at `0970e07a6403c868e20deef02d85b6956d8f4906`, tested at `7f9b09e9c08462b99d4043e16ed2cd00b1a1f1e6`, and recorded at `ef5c6c78ce86d4ee38a1a1c49e004a445b4727cb`.

It is `REJECTED_PAIRED_SCREEN`:

- union wealth `4.744148x -> 3.056690x` (-35.57%), MDD 30.56% -> 48.60%;
- common-five `7.904183x -> 10.904526x` (+37.96%), MDD 39.14% -> 44.60%;
- leader-removal `1.154679x -> 0.843213x` (-26.97%), MDD 35.33% -> 65.99%.

No 2026/final matrix was run. Do not tune or repackage it. Focused 6/6, ordinary 87/87, research 378 passed/9 skipped; maximum ledger residual `5.48e-9` CNY.

The generic `campaign_handover` diagnosis was also rejected before implementation: mean 10-session leader-minus-blocker spread was +0.45pp / -1.36pp / -0.31pp across union/common-five/removal. A first ordinary-exit intent audit found zero cross-scope activations for the proposed profitable-plus-peer-confirmed delay. Do not implement either.

## Next independent work

Persistent ownership proves that lifecycle length has major return leverage in common-five, but it carries broad-universe and removal distractors. Before another candidate:

1. Attribute actual treatment winning and losing episodes at entry and during their first causally observed lifecycle.
2. Test whether contemporaneous price acceleration, sector participation and market state provide a generic cross-scope campaign-quality discriminator.
3. Do not use future outcomes at runtime, add another exit exception, or search neighboring windows/thresholds.
4. Only adequate, consistent cross-scope evidence may authorize one preregistered standalone entry-plus-lifecycle engine.
5. Reject that fixed engine on any core wealth regression, missing strict union improvement, or lack of any strict improvement; only a pass may proceed to 2026/final checks.

Also do not repeat `causal_theme_campaign`, `leader_anchor_slots`, `campaign_payoff`, `joint_funding`, `confirmed_shock`, inventory/T+1 corrections, timing/fee probes, `profit_trend_shield` or `shock_reclaim`.

Writer state: all source, tests and fixed results from this batch are remotely saved. No hosted run or unpublished source is pending. Single-writer ownership is released. This is not task completion or an economic-budget reset.
