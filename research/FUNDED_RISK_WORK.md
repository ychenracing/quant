# Funded-book risk continuation

The support-risk study is complete but rejected for joint acceptance; its 180 manifest files, exact source tree and remote archive hash were verified. The concurrently written results/support_budget_economics.json is retained, not overwritten. Main is unchanged.

Follow funded_risk_contract.json within the original authorization. The next bounded comparison changes risk authority, not the original economic criteria: individual stops and actual-book modeled loss govern protective liquidation; the original warning state still freezes new commitments; one candidate adds a non-resetting actual-NAV loss budget. Exactly two candidates, fixed prior pre-2026 sizing selection. No new model fit or broad parameter sweep.

Implementation: add failing tests for warning freezes, hard risk ceilings, non-resetting NAV history, fill-aware protection and causal isolation. Factor only two explicit pure defaults from support_budget.Owner and verify its historical close decisions remain identical. Implement the new authority and identity-bound finite registration. Preserve source before the requested study; use the existing immutable archive publisher and read-back verification. Then inspect actual economic outcomes and continue evidence-supported work; neither engineering success nor a completed archive authorizes main promotion.

The separate tail-authority work in risk_reliability_contract.json remains authorized and intact. Do not overwrite its files, metadata or active requests. Coordinate changes to the finite runner from the latest remote branch and never force-update the branch.
