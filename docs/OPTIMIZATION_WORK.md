# Independent optimization work

## Authorization and recovery

2026-09-14: the user explicitly authorizes continuing independent strategy research, including rewriting, and saving intermediate code and evidence to GitHub. This continues PR #1 on `research/independent-tech`, not a fork of any of the four reference strategies. Initial remote head: `d2071ebd29f23d3abe6b61804b4925cec149b8b3`; initial main: `234cc5729455ce5440b0a5307aadbcb9c1c0bbc4`. A separate previous local delivery at `d14ef93df6661d876f2f9d1885136b2974310a31` is available in the uploaded source/Git and complete evidence archives. Its results do not prove remote HEAD.

## Scope

A-share technology securities only, from 2023-01-03; supplied frozen market ends 2026-09-11. Cash long-only, daily close decisions, next-session execution, human verification, no broker orders. Preserve all older evidence, numerical searches, failures and contracts; do not rewrite prior acceptance. Any new architecture/search receives a separate predeclared protocol and complete trial ledger. All periods already inspected are retrospective, not untouched out-of-sample data. No name/date-specific rules and no copied reference production engine.

## Investigation and implementation plan

1. Restore source and frozen input identities, reproduce the incumbent, and identify losses from selection, exits, missed trends, risk interventions and recovery separately.
2. Evaluate independent architectures which separate persistent trend ownership from rapid daily downside protection. Prefer fewer moving parts, fewer trades and globally shared parameters; retain execution/data safeguards.
3. Freeze each bounded candidate family before running it. Use earlier years for selection, report later-year and subset failures without using them to retroactively alter the frozen family. Expand research only as a newly logged experiment, never as a silent replacement.
4. Run matched engine/cost comparisons, key and realized-leader removals, original pools, cardinalities, deterministic subsets, costs/delay perturbations and explicit risk-signal/execution timing. Retain native-reference incompatibilities rather than labelling them equal-execution proof.
5. Save tested source and evidence on the original remote branch at each meaningful checkpoint; read back commit/file hashes. Keep CI short and offline. Update architecture, parameters, operating limits and economic status from actual results.

## Status

Recovery in progress. GitHub write capability is being exercised by this checkpoint. No new economic improvement is claimed by this document. The prior delivery's 476 replay records and all failed cases remain preserved in the uploaded complete archive.
