# 已完成结构检验的独立核验与原件保全

核验日期：2026-09-15。此目录只追加审核证据，不修改源码、原始测量、合同、候选集合或 main。
研究范围按 `decision-review/budget.json` 已保存的累计三假设约束执行，不因会话或分支切换重置。

## 经济结论

三项结构检验均已有真实配对结果，没有可以晋级的处理方案。

- 新开仓支撑筛选：`8ebcb5311f08f77d8e3077277126bd6bbd43504c` / `34915730072`，三个池的训练期财富均下降，选择器选回对照。
- 保护性减仓分配：`6da9e119af256f23c2b2be45fd3d4f5efa1a8bff` / `34918924019`，联合池及移除池财富下降，共同五股不变，选择器选回对照。
- 健康持仓资金补足：并行源码 `07f4f32f2a856866bb774e128470b437ceb7f779` / `34919407590`，三个池财富提高，但回撤及成交数均增加，未通过预登记的逐池财富/回撤非退化条件。

最后一项的固定训练区间为 2023-01-03 至 2025-12-31，727 个交易日：

| 范围 | 对照财富 | 处理财富 | 对照最大回撤 | 处理最大回撤 | 对照/处理成交笔数 |
|---|---:|---:|---:|---:|---:|
| 联合池 | 4.231186496672099 | 5.309349861908156 | 29.6451068365% | 32.2869947480% | 149 / 167 |
| 共同五股 | 5.718046146255741 | 6.326660007155455 | 31.5516349999% | 36.0293317099% | 118 / 124 |
| 联合移除三龙头 | 2.358205122900610 | 2.394041804132362 | 33.0299975654% | 37.0276599897% | 161 / 168 |

这说明资金补足并非没有收益作用，但不能以增加回撤为代价宣称联合目标通过。也不把局部支撑风险代理下降当成实际回撤下降。没有用三个方案按池拼接赢家，没有调阈值、追加邻近候选或开放第四个结构假设。

保护性减仓研究所选原对照在全期共同五股为 10.598717085973565 倍，仍低于同执行 incumbent 的 10.824786016250098 倍。新健康持仓处理在配对筛选失败后未运行全期矩阵；不能将训练期改善宣称为全期或原生参考胜出。原始经济目标仍为 NOT_MET，而不是一个已完成盈利能力改造。

## 实际核验范围

对减仓研究：核验源码树 `746729880db98c03f93f40dca2d05f63b9fb4839`、全部声明源码依赖、156 个归档清单成员；读取并复核 21 个账户记录、45 行窗口指标及 37 个减仓意图。现金、T+1、逐日盈亏对账、已存在数量上限、同额减仓及不增加盘后支撑风险的检查通过。最大逐日对账误差为 1.4901161193847656e-08 元。三个训练对照的 12 份经济文件与旧原件逐字节一致。87 项离线及 217 项研究测试的托管日志通过。

对并行健康持仓研究：从其归档重建源码树 `2b0e1b0e3911377c0f3232971c4e96ac007a9cd8`，核验声明源码依赖、90 个顶层清单成员及内部诊断清单；重新计算六个原始账户的指标和配对决策。660 个新增资金资格意图均只涉及已有持仓，没有引入被拒绝的减仓处理。三个对照的经济文件仍与旧对照原件相同。87 项离线及 240 项研究测试的托管日志通过。

这些是已有结果的核验，不是重新运行 27 个组合；本次独立审核没有新组合回放。不同账户记录和重叠对照也不构成独立统计样本。工程通过不等于经济通过。

## 必须保留的非等价

减仓研究中的原始诊断重建有四份文件的末位浮点不同，五份中只有联合池 CSV 的原始哈希一致。严格的 `DIAGNOSTIC_RESTORE_FAILED` 与首次审核失败日志均保留，不修改预登记的期望哈希，也不把数值接近标为字节等价。该失败导致工作流中后续的旧工程原件解码及旧清单恢复步骤跳过；不能宣称这些后续步骤在该托管运行中已经成功。

原来的五份本地观察原件仍存在。现在通过对不可变托管归档的无损字节差分保全它们：差分由实际两份文件计算，恢复后的每个字节流均与实际本地原件及测量前记录的 SHA256 匹配。错误归档拒绝和重复恢复幂等性已验证。原件、托管重建件和两者非等价记录分别保留；这不是修饰或修复经济测量。

## 文件恢复

`REVIEW_MANIFEST.json` 给出 `review-files.xz.b64`、解码后 JSON 和其中十二份原始文件的哈希。去掉 base64 空白后解码，再 xz 解压，得到文件名到原始文本的 UTF-8 JSON 映射。逐项保存并核对哈希，不依赖旧会话或未提交工作区。

其中 `restore_original_observations.py` 与 `original-observations.json` 配对使用，输入下列已发布归档和一个新输出目录，恢复五份最初的观察原件，不运行回测：

```text
python restore_original_observations.py nonlinear-evidence.tar.gz OUTPUT
```

输入必须来自 `nonlinear/6da9e119af256f23c2b2be45fd3d4f5efa1a8bff/34918924019/nonlinear-evidence.tar.gz`，SHA256 为 `1e2257add169e9599ff3ae6647d3dd02e8285431078fe357d6faf89da1396f64`。

并行配对归档为 `nonlinear/07f4f32f2a856866bb774e128470b437ceb7f779/34919407590/nonlinear-evidence.tar.gz`，SHA256 为 `cece9c0b39620d0de7c5b396fa45f0b7449a887d1696ce7e89c6b6789695b889`。

两份独立审核脚本保留了实际使用的本地恢复路径作为执行记录；可移植的原件恢复脚本不依赖这些路径。原始首次失败脚本、失败日志和完成审核日志全部保留。

## 未完成的原始目标

原生私有参考的完整原件恢复和执行有效性仍有已记录缺口；超时、无效覆盖或未恢复不算 quant 胜出。现有有限研究不证明任意股票池或未来最优；2026 是反复观察的回顾性历史。经济单位仍不是完整实际股份、公司行动及税务账本。

同一生产候选达到原始联合经济目标及适用验证尚未完成。三个已检验处理都不进入 main；保留当前生产默认，不把失败处理包装成发布。代码、审核与原始失败证据可追溯并不消除经济缺口。
